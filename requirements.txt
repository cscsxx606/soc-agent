# SOC Multi-Agent Flask 后端依赖（生产 pinned 版本）
# 已用 pip freeze 验证当前 venv 一致

# === Flask 框架 ===
Flask==3.1.3
flask-cors==6.0.5
Flask-Limiter==4.1.1
Flask-WTF==1.3.0
WTForms==3.2.2

# === 安全 / 鉴权 ===
PyJWT==2.13.0
bcrypt==5.0.0

# === 网络 / HTTP ===
requests==2.34.2

# === 终端 / 日志 ===
rich==14.3.4
PyYAML==6.0.3

# === 生产服务器 ===
gunicorn==26.0.0
gevent==26.5.0
cffi==2.1.0

# === 多 Agent 引擎（可选，未启用 fallback）===
crewai==1.11.1

# === 可选依赖（注释保留）===
# litellm  # CrewAI 多模型支持
# reportlab  # PDF 报告
# weasyprint  # HTML → PDF
